import React from 'react'

interface IProductListProps {
  
}

const ProductList: React.FunctionComponent = (props: IProductListProps) => {
  return (
    <div>ProductList</div>
  )
}

export default ProductList